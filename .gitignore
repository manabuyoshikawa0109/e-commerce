# 元の HTML テンプレート一式（webroot へ反映済みのため追跡しない）
#
# 先頭の / … リポジトリ直下のみに限定する。
#   これが無いと「どの階層の template という名前にも一致」してしまい、
#   macOS は大文字小文字を区別しないため src/src/Template/ まで巻き込まれる。
# 末尾の / … ディレクトリのみに一致させ、同名ファイルを巻き込まないようにする。
/template/

# OS generated files #
######################
.DS_Store
